const fs = require('fs'); const origReadlinkSync = fs.readlinkSync; fs.readlinkSync = function(...args) { try { return origReadlinkSync.apply(this, args); } catch (err) { if (err && err.code === 'EISDIR') err.code = 'EINVAL'; throw err; } }; const origReadlink = fs.readlink; fs.readlink = function(...args) { const cb = args[args.length - 1]; if (typeof cb === 'function') { args[args.length - 1] = function(err, result) { if (err && err.code === 'EISDIR') err.code = 'EINVAL'; cb(err, result); }; } return origReadlink.apply(this, args); }; if (fs.promises && fs.promises.readlink) { const origPromisesReadlink = fs.promises.readlink; fs.promises.readlink = async function(...args) { try { return await origPromisesReadlink.apply(this, args); } catch (err) { if (err && err.code === 'EISDIR') err.code = 'EINVAL'; throw err; } }; }
